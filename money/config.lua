config = {}

-- # cycle amount # --
config.cycle_length = 5 -- this is in minutes
config.cycle_amount = 100 -- how much the user will get by default
--config.enable_change_your_own_cycle = false -- enable /setsalary (amount)


config.deptPay = {
    ["Staff"] = 10000,
    ["LSPD"] = 5000,
    ["SAHP"] = 3500,
    ["BCSO"] = 3000,
    ["Civilian"] = 5000,
    ["SAFD"] = 4000
}
 

